banana

# 测试变量定义和输出
let name = "Banana Language"
println("欢迎使用 " + name)

# 测试数值运算
let a = 10
let b = 5
println("a = " + str(a))
println("b = " + str(b))
println("a + b = " + str(a + b))
println("a - b = " + str(a - b))
println("a * b = " + str(a * b))
println("a / b = " + str(a / b))
println("a % b = " + str(a % b))
println("a ^ b = " + str(a ^ b))

# 测试条件语句
if a > b
    println("a 大于 b")
else
    println("a 小于或等于 b")
endif

# 测试循环
let i = 0
while i < 5
    println("循环次数: " + str(i))
    set i = i + 1
endwhile

# 测试计数循环
loop(1, 10, 2)
    println("计数循环: " + str(i))
endloop

# 测试列表和循环
let fruits = ["苹果", "香蕉", "橙子", "葡萄"]
for fruit in fruits
    println("水果: " + fruit)
endfor

# 测试函数定义和调用
func greet(name)
    return "你好, " + name
endfunc

let greeting = call(greet, ["世界"])
println(greeting)

# 测试内置函数
println("随机数: " + str(rand()))
println("列表长度: " + str(len(fruits)))
println("最大值: " + str(max([1, 3, 5, 2, 4])))
println("最小值: " + str(min([1, 3, 5, 2, 4])))
println("总和: " + str(sum([1, 2, 3, 4, 5])))

# 测试输入
let age = input_num("请输入你的年龄: ") 
println("你输入的年龄是: " + str(age))

# 测试显示变量
show_var()

end
